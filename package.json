{
  "name": "matchday",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "dependencies": {
    "buffer": "^6.0.3",
    "sodium-javascript": "^0.8.0"
  }
}
